diff --git a/Makefile b/Makefile
index bd9515f..05bfd1b 100644
--- a/Makefile
+++ b/Makefile
@@ -36,7 +36,7 @@ RM= rm -f
 # == END OF USER SETTINGS -- NO NEED TO CHANGE ANYTHING BELOW THIS LINE =======
 
 # Convenience platforms targets.
-PLATS= aix ansi bsd freebsd generic linux macosx mingw posix solaris
+PLATS= aix ansi bsd freebsd generic linux macosx mingw posix solaris nacl-newlib nacl-newlib-basic nacl-glibc nacl-glibc-basic
 
 # What to install.
 TO_BIN= lua luac
diff --git a/src/Makefile b/src/Makefile
index fea895b..4f50b78 100644
--- a/src/Makefile
+++ b/src/Makefile
@@ -102,6 +102,18 @@ freebsd:
 
 generic: $(ALL)
 
+nacl-newlib:
+	$(MAKE) all MYCFLAGS="-DLUA_USE_POSIX -DLUA_USE_READLINE" MYLIBS="-lreadline -lhistory -lncurses -lnosys -lglibc-compat"
+
+nacl-newlib-basic:
+	$(MAKE) all MYCFLAGS=-DLUA_USE_POSIX
+
+nacl-glibc:
+	$(MAKE) all MYCFLAGS=-DLUA_USE_LINUX MYLIBS="-Wl,-E -ldl -lreadline -lhistory -lncurses"
+
+nacl-glibc-basic:
+	$(MAKE) all MYCFLAGS="-DLUA_USE_POSIX -DLUA_USE_DLOPEN" MYLIBS="-Wl,-E -ldl"
+
 linux:
 	$(MAKE) $(ALL) SYSCFLAGS="-DLUA_USE_LINUX" SYSLIBS="-Wl,-E -ldl -lreadline"
 
