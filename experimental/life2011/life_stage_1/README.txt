Life Tutorial Step 1.

In this step of the Life Tutorial, we set up a simple project that builds and
installs a NaCl-enabled web application.  We add some simple JavaScript to
load the NaCl module so that it displays "full-screen" and automatically
resizes itself as the browser window changes size.
